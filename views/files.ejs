<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Files</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Gilroy';
        }

        html,
        body {
            height: 100%;
            width: 100%;
        }

        #main {
            width: 100%;
            height: 100%;
            background: rgb(4, 4, 4);
            color: white;
            padding: 2rem;
            display: grid;
            gap: 10px;
            grid-template-columns: 35% 65%;
        }

        #list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            height: 100%;
            padding: 4rem;
            background: rgb(20, 20, 20);
            border-radius: 1rem;
        }

        .file {
            display: flex;
            justify-content: space-between;
            width: 100%;
            padding: 1rem;
            border-radius: 1rem;
            background: rgb(43, 43, 43);
            text-align: center;
            align-items: center;
        }

        .options {
            cursor: pointer;
            display: flex;
            gap: 1rem;
        }

        button {
            background: rgb(20, 20, 20);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 100px;
            cursor: pointer;
            transition: background 0.3s ease;

            &:hover {
                background: purple;
            }
        }


        a {
            text-align: center;
            background: rgb(20, 20, 20);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 100px;
            cursor: pointer;
            transition: background 0.3s ease;

            &:hover {
                background: purple;
            }

            color: white;
            text-decoration: none;
        }

        #note {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            width: 100%;
            font-size: 2rem;
        }

        #myfiles {
            background: rgb(20, 20, 20);
            border-radius: 1rem;
        }

        #redirect {
            background: rgb(77, 0, 77);

            &:hover {
                background: purple;
            }
        }
    </style>
</head>
<body>
    <div id="main">
        <div id="list">
            <a id="redirect" href="/form">Create File</a>
            <% data.forEach((value, index)=> { %>
                <div class="file">
                    <h2>
                        <%= value %>
                    </h2>
                    <div class="options">
                        <a href="/edit/<%= value %>">Edit</a>
                        <a href="/delete/<%= value %>">Delete</a>
                    </div>
                </div>
                <% })%>
                    <% if(data.length==0){%>
                        <div id="note">
                            <p>No Files Found</p>
                        </div>
                        <%}%>
        </div>

        <!-- <div id="myfiles">
            <%- include('./edit.ejs') %>
        </div> -->
    </div>
</body>

</html>